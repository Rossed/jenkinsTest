pipeline {
    agent any

    parameters {
            string(name: 'Username', defaultValue: 'Ross', description: 'How should I greet the world?')
        }
    stages {    
        stage('Example') {
            steps {
                echo "Hello ${params.Greeting}, we are building..."
            }
        }
        stage('Test') {
            steps {
                echo 'Testing..'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Deploying....'
            }
        }
    }
}